<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ztman.admin.mapper.SysMenuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ztman.admin.api.entity.SysMenu">
        <id column="menu_id" property="menuId"/>
        <result column="name" property="name"/>
        <result column="permission" property="permission"/>
        <result column="path" property="path"/>
        <result column="parent_id" property="parentId"/>
        <result column="icons" property="icons"/>
        <result column="icon" property="icon"/>
        <result column="component" property="component"/>
        <result column="sort" property="sort"/>
        <result column="type" property="type"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="del_flag" property="delFlag"/>
    </resultMap>

    <resultMap id="MenuVoResultMap" type="com.ztman.admin.api.vo.MenuVO">
        <id column="menu_id" property="menuId"/>
        <result column="name" property="name"/>
        <result column="permission" property="permission"/>
        <result column="parent_id" property="parentId"/>
        <result column="icons" property="icons"/>
        <result column="icon" property="icon"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="sort" property="sort"/>
        <result column="type" property="type"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="del_flag" property="delFlag"/>
        <result column="parent_name" property="parentName"/>
    </resultMap>

    <!--通过角色查询菜单信息-->
    <select id="findMenuByRoleCode" resultMap="MenuVoResultMap">
        SELECT
            sys_menu.*
        FROM
        sys_role
        LEFT JOIN sys_role_menu ON sys_role_menu.role_id = sys_role.role_id
        LEFT JOIN sys_menu ON sys_menu.menu_id = sys_role_menu.menu_id
        WHERE
        sys_role.del_flag = 1
        AND sys_menu.del_flag = 1
        <if test="role != null and role != ''">
            AND sys_role.role_code = #{role}
        </if>
        <if test="platformName!= null and platformName != ''">
            and sys_menu.platform_name=#{platformName}
        </if>
        ORDER BY sys_menu.sort DESC
    </select>

    <!--通过角色ID 查询权限-->
    <select id="findPermissionsByRoleIds" resultType="java.lang.String">
        SELECT
            m.permission
        FROM
        sys_menu m, sys_role_menu rm WHERE m.menu_id = rm.menu_id AND m.del_flag = 1 AND rm.role_id IN (#{roleIds})
    </select>

    <select id="selectWithMenuVoPage" resultMap="MenuVoResultMap">
        SELECT
        t.menu_id,
        t.name,
        t.permission,
        t.parent_id,
        t.icons,
        t.icon,
        t.path,
        t.component,
        t.sort,
        t.TYPE,
        t.create_time,
        t.update_time,
        t.del_flag, IFNULL(d.name, '菜单') AS parent_name
        FROM
        (
        SELECT
        menu.menu_id,
        menu.name,
        menu.permission,
        menu.parent_id,
        menu.icons,
        menu.icon,
        menu.path,
        menu.component,
        menu.sort, TYPE,
        menu.create_time,
        menu.update_time,
        menu.del_flag
        FROM sys_menu menu
        left join sys_role_menu rm on  rm.menu_id = menu.menu_id
        left join sys_role ro on ro.role_id = rm.role_id
        WHERE
        1 = 1
        and menu.del_flag = 1
        <if test="roleIds != null ">
            and ro.role_id in
            <foreach collection="roleIds" item="id" index="index" open="(" close=")" separator=",">
                #{id}
            </foreach>
        </if>
        <if test="parentId != null">
            AND menu.parent_id = #{parentId}
        </if>
        <if test="dataSearch != null and  dataSearch != ''">
            AND menu.name LIKE CONCAT('%','${dataSearch}','%')
        </if>
        ) t
        LEFT JOIN sys_menu d ON t.parent_id = d.menu_id
        ORDER BY t.menu_id
    </select>

    <select id="getChildrenOrgOfMenu" parameterType="java.lang.Integer" resultType="java.lang.Integer">
        SELECT menu_id FROM sys_menu WHERE FIND_IN_SET(parent_id,getChildrenOrgOfMenu(${_parameter}))
    </select>

    <select id="getParentOrgOfMenu" parameterType="java.lang.Integer" resultType="java.lang.Integer">
		SELECT menu_id FROM sys_menu WHERE FIND_IN_SET(menu_id,getParentOrgOfMenu(${_parameter}))
    </select>

    <select id="getMenuByType" parameterType="java.lang.Integer" resultMap="BaseResultMap">
		SELECT
		t.menu_id,
		t.name,
		t.permission,
		t.parent_id,
		t.icons,
		t.icon,
		t.path,
		t.component,
		t.sort,
		t.TYPE,
		t.create_time,
		t.update_time,
		t.del_flag
		FROM sys_menu t
		where 1 = 1
		and t.del_flag = 1
		and t.TYPE = #{_parameter}
	</select>
</mapper>
